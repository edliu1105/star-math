# 第二轮评审任务（里程碑③ 首个可玩版本 + 里程碑④ 对第一轮的逐条回应）

## 先读这些

1. `docs/REVIEW_BRIEF.md` —— 你的评审职责、六个维度、iOS 军规 checklist、输出格式（**必须遵守**）
2. `docs/RESPONSE-R1.md` —— 我对你第一轮 REJECT 的**逐条回应**（含 4 条书面反驳）
3. `index.html` —— 全部实现（单文件：CSS + 引擎 + 六个游戏 + 主控），约 2400 行
4. `sw.js` / `manifest.webmanifest` —— PWA
5. `tests/test_app.py` —— 发布门槛测试
6. **测试证据**（请以这两个"已跑完"的日志为准）：
   - `shots/test-chromium-pass136.log` —— Chromium 全量门槛 **136 通过 / 0 失败 / 0 警告**（808 秒，八个组别 A–H，六世界 × 四难度）
   - `shots/test-wrong.log` —— 答错路径专项 **14 通过 / 0 失败**（六世界 × L2/L4 全程故意答错）
   - `shots/test-chromium.log` / `shots/test-webkit.log` —— 复核轮，正在跑，可能是**半截文件**，不要据此下结论
   - 说明：`pass136` 那一轮之后只再改了两处低风险内容（地图从"每次重建"改为"只刷新状态"、「再说一遍」按钮按当前步骤播报），
     都已用 `tests/smoke.py` 四个难度 × 六个世界复验过零 console error
7. `docs/IPAD-CHECKLIST.md` —— 交给委托方在真机执行的验收清单

## 本轮要求

**不要采信 `RESPONSE-R1.md` 的任何说法，逐条到 `index.html` 里核对。** 我在回应里声称做到的每一件事，请你在代码里找到对应实现；找不到或实现有漏洞的，按"未修复"处理并明确指出行为/函数名。

请特别攻击下面这些点（都是你第一轮点名的死穴）：

1. **猜测通关是否真的被封死。** 逐个玩法推演："一个只会乱点、完全不理解数量的三岁半孩子，能不能通关？能不能升级？" 特别看：
   - `GAMES.bluey` 守恒轮答错后的路径（`pick2` → `handPair` → `build(true)`）
   - `GAMES.monkey` 的 `tapStone` / `submit`（能不能一路点到底）
   - `GAMES.peppa` 的 `apat !== qpat` 是否真的排除了所有相同排列的情况
   - `Store.evidence()` 的滑动窗口是否真的能拦住"猜对 4/5"
   - `st.isRetry` / `st.hinted` 是否覆盖了所有"提示后作答"的路径
2. **分技能纠错是否真的没把目标技能退化掉。** 尤其：瞬识纠错是否真的只用组块、没有偷偷逐个数；接着数纠错是否真的保留了已有长度。
3. **语音双通道在 WebKit / iOS 上的失败模式。** `Speech.say()` 与 `Speech.count()` 之间、`unlockAndSay()` 与 `voiceschanged` 补说之间，还有没有会导致队列卡死、掐断、或重复播报的竞态？逐条核对九条军规在代码里的落点。
4. **生命周期与竞态。** `SESSION` 令牌 + `ctx.alive()` + `Timers` 是否真的覆盖了所有异步回调？有没有漏网的 `setTimeout` / 监听器 / canvas 状态？
5. **三岁半可用性。** 看截图：可点目标够不够大、当前该点哪里是否唯一明确、分级提示会不会打扰、成人门是否足够难。
6. **我反驳的 4 条**（本地录音兜底 / IP 确认时机 / 不做统一 rAF 调度 / 真机验收交给委托方）**是否接受**。不接受就说清楚为什么，并给出你认为可行的替代方案。

## 附带的三张截图

- 图1：横屏 · 佩奇泥坑（瞬识关，泥点闪现中）
- 图2：横屏 · Bluey 守恒关（两排等量、只有间距不同）
- 图3：竖屏 · 世界地图（先修门控 + 星星进度）

## 输出

严格按 `docs/REVIEW_BRIEF.md` 的六维度格式，问题按严重度编号，每条给可执行建议。
**最后一行必须是** `VERDICT: APPROVE` 或 `VERDICT: REJECT`。

如果你认为可以 APPROVE，请同时列出"上线前最后必须人工确认的事项"（我会放进真机清单）。
